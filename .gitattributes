# LF everywhere in the repository. Otherwise files diverge between Windows
# and macOS, and diffs show the whole file instead of the actual change.
* text=auto eol=lf

# Binary files stay untouched.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.pdf binary
